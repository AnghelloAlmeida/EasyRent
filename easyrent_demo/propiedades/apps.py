from django.apps import AppConfig

class PropiedadesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'propiedades' # El nombre de tu aplicación debe coincidir con la carpeta